title: AuthToken
type: object
properties:
  access_token:
    type: string
    minLength: 1
  expires_in:
    type: integer
  token_type:
    type: string
    minLength: 1
required:
  - access_token
  - expires_in
  - token_type
